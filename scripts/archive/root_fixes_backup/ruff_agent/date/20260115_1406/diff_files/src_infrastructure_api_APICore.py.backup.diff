diff --git a/src/infrastructure/api/APICore.py b/src/infrastructure/api/APICore.py
index 739476e..6a91c70 100644
--- a/src/infrastructure/api/APICore.py
+++ b/src/infrastructure/api/APICore.py
@@ -31,12 +31,24 @@ from src.core.base.version import SDK_VERSION
 
 __version__ = VERSION
 
+try:
+    import rust_core as rc
+except ImportError:
+    rc = None
+
 class APICore:
+    """Logic for API-related operations, including OpenAPI schema generation."""
     def __init__(self, version: str = SDK_VERSION) -> None:
         self.version = version
 
     def build_openapi_json(self, tool_definitions: list[dict[str, Any]]) -> str:
         """Constructs an OpenAPI 3.0 string from tool metadata."""
+        if rc:
+            try:
+                return rc.generate_openapi_spec(tool_definitions, self.version)
+            except Exception:
+                pass
+
         paths = {}
         for tool in tool_definitions:
             tool_name = tool.get('name', 'unknown')
