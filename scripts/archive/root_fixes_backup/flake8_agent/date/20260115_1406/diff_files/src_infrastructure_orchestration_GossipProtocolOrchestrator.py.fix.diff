diff --git a/src/infrastructure/orchestration/GossipProtocolOrchestrator.py b/src/infrastructure/orchestration/GossipProtocolOrchestrator.py
index 86152d4..8995e9a 100644
--- a/src/infrastructure/orchestration/GossipProtocolOrchestrator.py
+++ b/src/infrastructure/orchestration/GossipProtocolOrchestrator.py
@@ -17,7 +17,7 @@ import asyncio
 import logging
 import random
 import time
-from typing import Dict, Any, Optional, Set, TYPE_CHECKING
+from typing import Any, TYPE_CHECKING
 from src.core.base.version import VERSION
 
 if TYPE_CHECKING:
@@ -25,6 +25,16 @@ if TYPE_CHECKING:
 
 __version__ = VERSION
 
+
+
+
+
+
+
+
+
+
+
 class GossipProtocolOrchestrator:
     """
     Handles state synchronization across the swarm using an epidemic (gossip) protocol.
@@ -82,7 +92,7 @@ class GossipProtocolOrchestrator:
 
                 async with self._lock:
                     peers_list = list(self.peers)
-                
+
                 target_peer = random.choice(peers_list)
                 await self._synchronize_with_peer(target_peer)
             except asyncio.CancelledError:
@@ -96,7 +106,7 @@ class GossipProtocolOrchestrator:
         Exchanges digests (versions) and requests missing data.
         """
         logging.debug(f"Gossip: Syncing with {peer_name}...")
-        
+
         async with self._lock:
             # Randomly 'learn' something from the virtual peer to simulate convergence
             if random.random() > 0.7:
@@ -109,4 +119,4 @@ class GossipProtocolOrchestrator:
     async def get_synced_state(self, key: str) -> Any | None:
         """Returns the current state for a key."""
         async with self._lock:
-            return self.state.get(key)
\ No newline at end of file
+            return self.state.get(key)
