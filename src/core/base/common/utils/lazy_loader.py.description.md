# Description: src/core/base/common/utils/lazy_loader.py

Module docstring (if present):

    LazyLoader - Lazy module loading utilities.

    Inspired by vLLM's __getattr__ pattern for reducing import costs.
    Only loads modules when they are actually accessed.

    Phase 17: vLLM Pattern Integration (P2)

Top-level members:
- Classes: LazyModule, LazyImport, DeferredImport
- Functions: lazy_import, optional_import, require_import

Notes:
- This description was auto-generated by scripts/generate_docs_for_src.py
