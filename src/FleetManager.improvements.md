# Improvements: `FleetManager.py`

## Suggested improvements

- Function `__getattr__` is missing type annotations.
- Function `__init__` is missing type annotations.
- Function `register_agent` is missing type annotations.
- Function `register_remote_node` is missing type annotations.

## Notes
- These are suggestions based on static inspection; validate behavior with tests / runs.
- File: `src\classes\fleet\FleetManager.py`