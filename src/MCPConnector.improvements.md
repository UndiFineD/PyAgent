# Improvements: `MCPConnector.py`

## Suggested improvements

- Function `__init__` is missing type annotations.
- Function `_read_stderr` is missing type annotations.
- Function `start` is missing type annotations.
- Function `stop` is missing type annotations.

## Notes
- These are suggestions based on static inspection; validate behavior with tests / runs.
- File: `src\classes\fleet\MCPConnector.py`