# Improvements: `PluginManager.py`

## Suggested improvements

- Function `__init__` is missing type annotations.
- Function `discover_plugins` is missing type annotations.
- Function `load_plugin` is missing type annotations.

## Notes
- These are suggestions based on static inspection; validate behavior with tests / runs.
- File: `src\classes\fleet\PluginManager.py`