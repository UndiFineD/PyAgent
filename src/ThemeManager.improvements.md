# Improvements: `ThemeManager.py`

## Suggested improvements

- Function `__init__` is missing type annotations.
- Function `apply_theme` is missing type annotations.
- Function `refresh_widgets` is missing type annotations.
- Function `toggle_theme` is missing type annotations.

## Notes
- These are suggestions based on static inspection; validate behavior with tests / runs.
- File: `src\classes\gui\ThemeManager.py`