# Improvements: `AgentDashboard.py`

## Suggested improvements

- Function `__init__` is missing type annotations.
- Function `collapse_all` is missing type annotations.
- Function `expand_all` is missing type annotations.
- Function `setup_ui` is missing type annotations.

## Notes
- These are suggestions based on static inspection; validate behavior with tests / runs.
- File: `src\classes\gui\AgentDashboard.py`