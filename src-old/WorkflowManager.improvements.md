# Improvements: `WorkflowManager.py`

## Suggested improvements

- Function `__init__` is missing type annotations.
- Function `execute_current_phase` is missing type annotations.
- Function `finish_workflow` is missing type annotations.
- Function `get_agents_for_phase` is missing type annotations.
- Function `next_phase` is missing type annotations.
- Function `start_workflow` is missing type annotations.

## Notes
- These are suggestions based on static inspection; validate behavior with tests / runs.
- File: `src\classes\gui\WorkflowManager.py`