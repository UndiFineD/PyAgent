# Improvements: `AgentManager.py`

## Suggested improvements

- Function `__init__` is missing type annotations.
- Function `add_agent` is missing type annotations.
- Function `assign_file_to_available_agent` is missing type annotations.
- Function `clear_all` is missing type annotations.
- Function `get_agent_by_name` is missing type annotations.
- Function `load_state` is missing type annotations.
- Function `remove_agent` is missing type annotations.
- Function `save_state` is missing type annotations.

## Notes
- These are suggestions based on static inspection; validate behavior with tests / runs.
- File: `src\classes\gui\AgentManager.py`