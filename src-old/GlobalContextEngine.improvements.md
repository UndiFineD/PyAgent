# Improvements: `GlobalContextEngine.py`

## Suggested improvements

- Function `add_constraint` is missing type annotations.
- Function `add_entity_info` is missing type annotations.
- Function `add_fact` is missing type annotations.
- Function `add_insight` is missing type annotations.
- Function `consolidate_episodes` is missing type annotations.
- Function `load` is missing type annotations.
- Function `record_lesson` is missing type annotations.
- Function `save` is missing type annotations.

## Notes
- These are suggestions based on static inspection; validate behavior with tests / runs.
- File: `src\classes\context\GlobalContextEngine.py`