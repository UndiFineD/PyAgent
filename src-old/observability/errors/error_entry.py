#!/usr/bin/env python3
"""LLM_CONTEXT_START

## Source: src-old/observability/errors/error_entry.description.md

# Description: src/observability/errors/error_entry.py

Module docstring (if present):

    Auto-extracted class from agent_errors.py

Top-level members:
- Classes: ErrorEntry

Notes:
- This description was auto-generated by scripts/generate_docs_for_src.py
## Source: src-old/observability/errors/error_entry.improvements.md

# Improvements: src/observability/errors/error_entry.py

Suggested improvements (automatically generated):
- Add unit tests covering core behavior and edge cases.
- Break large modules into smaller, testable components.
- Avoid heavy imports at module import time; import lazily where appropriate.
- Add type hints and explicit return types for public functions.
- Add logging and better error handling for file and IO operations.
- Consider dependency injection for filesystem and environment interactions.

LLM_CONTEXT_END

"""

from __future__ import annotations

# Copyright 2026 PyAgent Authors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.


"""Auto-extracted class from agent_errors.py"""


import hashlib
from dataclasses import dataclass, field

from src.core.base.lifecycle.version import VERSION

from .error_category import ErrorCategory
from .error_severity import ErrorSeverity

__version__ = VERSION


@dataclass
class ErrorEntry:
    """A single error entry."""

    id: str = ""
    message: str = ""
    file_path: str = ""
    line_number: int = 0
    # Compatibility: older tests/callers pass error_type.
    error_type: str = ""
    severity: ErrorSeverity = ErrorSeverity.MEDIUM
    category: ErrorCategory = ErrorCategory.OTHER
    timestamp: str = ""
    stack_trace: str = ""
    suggested_fix: str = ""
    resolved: bool = False
    resolution_timestamp: str = ""
    tags: list[str] = field(default_factory=lambda: [])

    def __post_init__(self) -> None:
        if not self.id:
            seed = f"{self.error_type}|{self.message}|{self.file_path}|{self.line_number}".encode()
            self.id = hashlib.sha256(seed).hexdigest()[:12]

        if self.error_type and self.category == ErrorCategory.OTHER:
            et = self.error_type.lower()
            if "security" in et or "auth" in et:
                self.category = ErrorCategory.SECURITY
            elif "type" in et:
                self.category = ErrorCategory.TYPE
            elif "syntax" in et:
                self.category = ErrorCategory.SYNTAX
            elif "value" in et:
                self.category = ErrorCategory.VALUE
            elif "import" in et:
                self.category = ErrorCategory.IMPORT
