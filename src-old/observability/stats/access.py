#!/usr/bin/env python3
"""
LLM_CONTEXT_START

## Source: src-old/observability/stats/access.description.md

# Description: src/observability/stats/access.py

Module docstring (if present):

    Access.py module.

Top-level members:
- Classes: StatsAccessController

Notes:
- This description was auto-generated by scripts/generate_docs_for_src.py
## Source: src-old/observability/stats/access.improvements.md

# Improvements: src/observability/stats/access.py

Suggested improvements (automatically generated):
- Add unit tests covering core behavior and edge cases.
- Break large modules into smaller, testable components.
- Avoid heavy imports at module import time; import lazily where appropriate.
- Add type hints and explicit return types for public functions.
- Add logging and better error handling for file and IO operations.
- Consider dependency injection for filesystem and environment interactions.

LLM_CONTEXT_END
"""

from __future__ import annotations

"""
Access.py module.
"""
# Copyright 2026 PyAgent Authors
# Access control for stats.


import fnmatch


class StatsAccessController:
    """Controls access to stats."""

    def __init__(self) -> None:
        self.permissions: dict[str, dict[str, str]] = {}

    def grant(self, user: str, pattern: str, level: str = "read") -> None:
        self.permissions.setdefault(user, {})[pattern] = level

    def can_access(self, user: str, resource: str, required_level: str = "read") -> bool:
        if user not in self.permissions:
            return False
        req = required_level.lower()
        for pat, granted in self.permissions[user].items():
            if fnmatch.fnmatch(resource, pat):
                g = granted.lower()
                if req == "read" and g in ("read", "write"):
                    return True
                if req == "write" and g == "write":
                    return True
        return False
