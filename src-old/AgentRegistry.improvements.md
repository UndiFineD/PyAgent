# Improvements: `AgentRegistry.py`

## Suggested improvements

- Function `__contains__` is missing type annotations.
- Function `__getitem__` is missing type annotations.
- Function `__init__` is missing type annotations.
- Function `_instantiate` is missing type annotations.
- Function `get` is missing type annotations.
- Function `update` is missing type annotations.

## Notes
- These are suggestions based on static inspection; validate behavior with tests / runs.
- File: `src\classes\fleet\AgentRegistry.py`