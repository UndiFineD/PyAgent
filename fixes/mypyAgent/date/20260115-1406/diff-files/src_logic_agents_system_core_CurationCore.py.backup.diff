diff --git a/src/logic/agents/system/core/CurationCore.py b/src/logic/agents/system/core/CurationCore.py
index cf50296..37b3770 100644
--- a/src/logic/agents/system/core/CurationCore.py
+++ b/src/logic/agents/system/core/CurationCore.py
@@ -9,6 +9,7 @@ import shutil
 import time
 
 class CurationCore:
+    """Core logic for pruning and managing filesystem resources."""
     @staticmethod
     def prune_directory(directory: str, max_age_days: int = 7) -> int:
         """
@@ -18,6 +19,12 @@ class CurationCore:
         if not os.path.exists(directory):
             return 0
             
+        try:
+            import rust_core
+            return rust_core.prune_directory_rust(directory, max_age_days)
+        except (ImportError, AttributeError):
+            pass
+
         count = 0
         now = time.time()
         max_age_seconds = max_age_days * 86400
@@ -39,6 +46,15 @@ class CurationCore:
         """
         Forcefully removes all __pycache__ folders.
         """
+        if not os.path.exists(root_dir):
+            return 0
+
+        try:
+            import rust_core
+            return rust_core.deep_clean_pycache_rust(root_dir)
+        except (ImportError, AttributeError):
+            pass
+
         count = 0
         for root, dirs, files in os.walk(root_dir):
             if "__pycache__" in dirs:
