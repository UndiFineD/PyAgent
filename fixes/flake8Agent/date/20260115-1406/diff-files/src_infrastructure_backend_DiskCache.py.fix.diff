diff --git a/src/infrastructure/backend/DiskCache.py b/src/infrastructure/backend/DiskCache.py
index 154bc4e..4c545a4 100644
--- a/src/infrastructure/backend/DiskCache.py
+++ b/src/infrastructure/backend/DiskCache.py
@@ -26,10 +26,19 @@ import json
 import logging
 import time
 from pathlib import Path
-from typing import Optional
 
 __version__ = VERSION
 
+
+
+
+
+
+
+
+
+
+
 class DiskCache:
     """A simple disk-based cache for persistent storage of AI responses."""
 
@@ -88,7 +97,7 @@ class DiskCache:
 
         try:
             data = json.loads(file_path.read_text(encoding="utf-8"))
-            
+
             # Check TTL
             if self.ttl_seconds is not None:
                 timestamp = data.get("timestamp", 0)
@@ -99,7 +108,7 @@ class DiskCache:
                     except Exception:
                         logging.debug(f"Failed to unlink expired cache file {file_path}")
                     return None
-            
+
             return data.get("value")
         except Exception as e:
             logging.warning(f"Failed to read cache file {file_path}: {e}")
@@ -112,4 +121,4 @@ class DiskCache:
                 file_path.unlink()
             logging.debug(f"Cleared disk cache at {self.cache_dir}")
         except Exception as e:
-            logging.error(f"Failed to clear disk cache: {e}")
\ No newline at end of file
+            logging.error(f"Failed to clear disk cache: {e}")
