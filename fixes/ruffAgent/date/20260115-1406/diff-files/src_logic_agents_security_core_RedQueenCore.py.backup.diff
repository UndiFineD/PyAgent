diff --git a/src/logic/agents/security/core/RedQueenCore.py b/src/logic/agents/security/core/RedQueenCore.py
index 2981193..7b4b562 100644
--- a/src/logic/agents/security/core/RedQueenCore.py
+++ b/src/logic/agents/security/core/RedQueenCore.py
@@ -4,8 +4,15 @@ import re
 from typing import List
 from dataclasses import dataclass
 
+try:
+    import rust_core as rc
+    HAS_RUST = True
+except ImportError:
+    HAS_RUST = False
+
 @dataclass(frozen=True)
 class AttackVector:
+    """Represents a simulated adversarial pattern for stress-testing guardrails."""
     pattern: str
     target_guardrail: str
     success_rate: float
@@ -35,6 +42,11 @@ class RedQueenCore:
 
     def evaluate_bypass(self, response: str, forbidden_patterns: list[str]) -> float:
         """Scores a bypass attempt based on how much forbidden content leaked."""
+        if HAS_RUST:
+            try:
+                return rc.evaluate_bypass(response, forbidden_patterns)
+            except Exception:
+                pass
         matches = 0
         for pattern in forbidden_patterns:
             if re.search(pattern, response, re.IGNORECASE):
