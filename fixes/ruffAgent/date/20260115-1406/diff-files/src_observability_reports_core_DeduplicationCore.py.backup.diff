diff --git a/src/observability/reports/core/DeduplicationCore.py b/src/observability/reports/core/DeduplicationCore.py
index 7f4a87d..0f5ef62 100644
--- a/src/observability/reports/core/DeduplicationCore.py
+++ b/src/observability/reports/core/DeduplicationCore.py
@@ -7,12 +7,23 @@ Handles similarity calculations and JSONL export.
 import json
 from typing import List, Dict, Any
 
+try:
+    import rust_core as rc
+except ImportError:
+    rc = None
+
 class DeduplicationCore:
     @staticmethod
     def jaccard_similarity(s1: str, s2: str) -> float:
         """
         Calculates Jaccard similarity between two strings based on words.
         """
+        if rc:
+            try:
+                return rc.calculate_jaccard_similarity(s1, s2)
+            except Exception:
+                pass
+
         set1 = set(s1.lower().split())
         set2 = set(s2.lower().split())
         if not set1 or not set2:
@@ -26,6 +37,14 @@ class DeduplicationCore:
         """
         Removes items that are too similar to already seen items.
         """
+        if rc:
+            try:
+                messages = [item.get(key, "") for item in items]
+                unique_indices = rc.deduplicate_by_similarity(messages, threshold)
+                return [items[i] for i in unique_indices]
+            except Exception:
+                pass
+
         unique_items = []
         seen_messages: list[str] = []
         
