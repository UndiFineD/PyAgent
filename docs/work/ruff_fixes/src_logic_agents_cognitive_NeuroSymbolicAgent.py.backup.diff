diff --git a/src/logic/agents/cognitive/NeuroSymbolicAgent.py b/src/logic/agents/cognitive/NeuroSymbolicAgent.py
index 134e13f..d7a89db 100644
--- a/src/logic/agents/cognitive/NeuroSymbolicAgent.py
+++ b/src/logic/agents/cognitive/NeuroSymbolicAgent.py
@@ -48,7 +48,7 @@ class NeuroSymbolicAgent(BaseAgent):
         )
 
     @as_tool
-    def verify_and_correct(self, content: str) -> dict[str, Any]:
+    def perform_neurosymbolic_verification(self, content: str) -> dict[str, Any]:
         """
         Validates content against symbolic rules and attempts to flag violations.
         """
@@ -72,5 +72,5 @@ class NeuroSymbolicAgent(BaseAgent):
         }
 
     def improve_content(self, prompt: str) -> str:
-        res = self.verify_and_correct(prompt)
+        res = self.perform_neurosymbolic_verification(prompt)
         return res["corrected_content"]
\ No newline at end of file
