diff --git a/src/core/base/models/enums.py b/src/core/base/models/enums.py
index 60b0210..152fe9b 100644
--- a/src/core/base/models/enums.py
+++ b/src/core/base/models/enums.py
@@ -17,6 +17,11 @@
 from __future__ import annotations
 from enum import Enum, auto
 
+
+
+
+
+
 class AgentState(Enum):
     """Agent lifecycle states."""
     INITIALIZED = "initialized"
@@ -29,6 +34,11 @@ class AgentState(Enum):
     COMPLETED = "completed"
     ERROR = "error"
 
+
+
+
+
+
 class ResponseQuality(Enum):
     """AI response quality levels."""
     EXCELLENT = 5
@@ -37,6 +47,11 @@ class ResponseQuality(Enum):
     POOR = 2
     INVALID = 1
 
+
+
+
+
+
 class EventType(Enum):
     """Agent event types for hooks."""
     PRE_READ = "pre_read"
@@ -47,6 +62,11 @@ class EventType(Enum):
     POST_WRITE = "post_write"
     ERROR = "error"
 
+
+
+
+
+
 class AuthMethod(Enum):
     """Authentication methods for backends."""
     NONE = "none"
@@ -57,6 +77,11 @@ class AuthMethod(Enum):
     OAUTH2 = "oauth2"
     CUSTOM = "custom"
 
+
+
+
+
+
 class SerializationFormat(Enum):
     """Custom serialization formats."""
     JSON = "json"
@@ -66,6 +91,11 @@ class SerializationFormat(Enum):
     PROTOBUF = "protobuf"
     CBOR = "cbor"
 
+
+
+
+
+
 class FilePriority(Enum):
     """File priority levels for request prioritization."""
     CRITICAL = 5
@@ -74,6 +104,11 @@ class FilePriority(Enum):
     LOW = 2
     BACKGROUND = 1
 
+
+
+
+
+
 class InputType(Enum):
     """Input types for multimodal support."""
     TEXT = "text"
@@ -83,6 +118,11 @@ class InputType(Enum):
     AUDIO = "audio"
     VIDEO = "video"
 
+
+
+
+
+
 class AgentType(Enum):
     """Agent type classifications."""
     GENERAL = "general"
@@ -91,18 +131,33 @@ class AgentType(Enum):
     TESTING = "testing"
     REFACTORING = "refactoring"
 
+
+
+
+
+
 class MessageRole(Enum):
     """Roles for conversation messages."""
     USER = "user"
     ASSISTANT = "assistant"
     SYSTEM = "system"
 
+
+
+
+
+
 class AgentEvent(Enum):
     """Agent event types."""
     START = "start"
     COMPLETE = "complete"
     ERROR = "error"
 
+
+
+
+
+
 class AgentExecutionState(Enum):
     """Execution state for an agent run."""
     PENDING = auto()
@@ -112,6 +167,11 @@ class AgentExecutionState(Enum):
     CANCELLED = auto()
     PAUSED = auto()
 
+
+
+
+
+
 class AgentPriority(Enum):
     """Priority level for agent execution."""
     CRITICAL = 1
@@ -120,6 +180,11 @@ class AgentPriority(Enum):
     LOW = 4
     BACKGROUND = 5
 
+
+
+
+
+
 class ConfigFormat(Enum):
     """Configuration file format."""
     YAML = auto()
@@ -127,6 +192,11 @@ class ConfigFormat(Enum):
     JSON = auto()
     INI = auto()
 
+
+
+
+
+
 class DiffOutputFormat(Enum):
     """Output format for diff preview."""
     UNIFIED = auto()      # Unified diff format
@@ -134,6 +204,11 @@ class DiffOutputFormat(Enum):
     SIDE_BY_SIDE = auto()  # Side by side diff
     HTML = auto()         # HTML formatted diff
 
+
+
+
+
+
 class HealthStatus(Enum):
     """Health status for components."""
     HEALTHY = auto()
@@ -141,12 +216,22 @@ class HealthStatus(Enum):
     UNHEALTHY = auto()
     UNKNOWN = auto()
 
+
+
+
+
+
 class LockType(Enum):
     """File locking type."""
     SHARED = auto()       # Multiple readers allowed
     EXCLUSIVE = auto()    # Single writer only
     ADVISORY = auto()     # Advisory lock (not enforced by OS)
 
+
+
+
+
+
 class RateLimitStrategy(Enum):
     """Rate limiting strategy for API calls."""
     FIXED_WINDOW = auto()      # Fixed time window rate limiting
