diff --git a/src/observability/improvements/SLAManager.py b/src/observability/improvements/SLAManager.py
index d31c602..bab4d8e 100644
--- a/src/observability/improvements/SLAManager.py
+++ b/src/observability/improvements/SLAManager.py
@@ -27,10 +27,20 @@ from .SLAConfiguration import SLAConfiguration
 from .SLALevel import SLALevel
 from .SLAPolicy import SLAPolicy
 from datetime import datetime, timedelta
-from typing import Any, Dict, List, Optional
+from typing import Any
 
 __version__ = VERSION
 
+
+
+
+
+
+
+
+
+
+
 class SLAManager:
     """Manages SLAs for improvements.
 
@@ -77,7 +87,7 @@ class SLAManager:
             else:
                 try:
                     created_dt = datetime.fromisoformat(str(created))
-                except Exception:
+                except (ValueError, TypeError):
                     created_dt = None
 
             if not created_dt:
@@ -156,4 +166,4 @@ class SLAManager:
         if not self.tracked:
             return 100.0
         breached = len(self.get_breached())
-        return ((len(self.tracked) - breached) / len(self.tracked)) * 100
\ No newline at end of file
+        return ((len(self.tracked) - breached) / len(self.tracked)) * 100
