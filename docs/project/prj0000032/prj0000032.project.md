# prj0000032 — Agents Package

**Project ID:** `prj0000032`
**Short name:** agents
**Branch:** `prj0000032-agents`
**Status:** COMPLETE

## Overview

Defines the agent design and quality framework for PyAgent. Provides the
`src/agents/` package with `BaseAgent`, `AgentLifecycle`, and `AgentManifest`
as the foundational abstractions all concrete agents must implement.

## Branch Plan

**Expected branch:** `prj0000032-agents`
**Scope boundary:** `src/agents/`, `tests/agents/`, `docs/project/prj0000032/`

## Links

- Plan: plan.md
- Design: brainstorm.md

## Implementation

### `src/agents/BaseAgent.py`
- `AgentManifest` dataclass — name, version, description, capabilities, agent_id (UUID)
- `AgentLifecycle` enum — IDLE → RUNNING → STOPPED lifecycle state machine
- `BaseAgent` ABC — abstract `run(task)`, concrete `start/stop/reset`, concurrency
  semaphore-based `dispatch()`, `validate()` self-check

### `src/agents/__init__.py`
Re-exports `BaseAgent`, `AgentLifecycle`, `AgentManifest`.

### `tests/agents/test_base_agent.py`
13 passing tests covering manifest defaults, lifecycle transitions, error raises,
async dispatch, concurrency ordering, validate(), and repr.

## Tasks

- [x] `src/agents/BaseAgent.py` — BaseAgent ABC with lifecycle + dispatch
- [x] `src/agents/__init__.py` — package exports
- [x] `tests/agents/test_base_agent.py` — 13 passing tests
- [x] Project documentation

## Status

4 of 4 tasks completed
  (This is a heuristic; adjust project topic naming if needed.)