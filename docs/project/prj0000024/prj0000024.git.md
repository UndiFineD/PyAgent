# prj0000024 — Async Runtime — Git Handoff

## Branch Plan

**Expected branch:** `prj0000024-async-runtime`
**Observed branch:** `prj0000024-async-runtime`
**Project match:** ✅ YES

## Branch Validation

Branch `prj0000024-async-runtime` matches the project identifier `prj0000024`.
No branch naming violations detected.

## Scope Validation

Modified files:
- `src/core/runtime.py` — full async API: `spawn_task`, `set_timeout`, `create_queue`, `Runtime.submit/cancel/pending`
- `tests/runtime/test_runtime_import.py` — 10 tests covering all runtime functions
- `rust_core/src/async_runtime.rs` — Tokio singleton scaffold (T-0, raises PyNotImplementedError until T-1 wires the bridge)

## Failure Disposition

All 10 tests pass. Rust scaffold compiles (cargo check passes) but raises PyNotImplementedError — intentional for T-0.

## Lessons Learned

- `asyncio.ensure_future()` is the correct primitive for fire-and-forget task spawning in Python; `create_task` requires a running event loop reference.
- The Rust `once_cell::sync::Lazy<TokioRuntime>` pattern is the canonical singleton approach for the global Tokio runtime in PyO3 modules.
- `set_timeout` takes a `callable → coro` factory, not a coroutine, so the delay wrapper can call it correctly.
