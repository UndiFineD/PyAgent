# documentation-assets — Think Notes
_Status: COMPLETE_

The original `docs/tools.md` used a bullet list with outdated descriptions (e.g., `remote` described as "placeholder for SSH/FTP helpers" after it was fully implemented with `run_ssh_command`/`upload_file`). The `plugin_loader` module was missing entirely. Updated to a Markdown table format listing all 16 tools with accurate descriptions and the `pm/` subpackage.
