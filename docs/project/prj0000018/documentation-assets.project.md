# documentation-assets — Project Overview

## Project Identity
**Project ID:** prj0000018
**Short name:** documentation-assets
**Project folder:** `docs/project/prj0000018/`

## Project Overview
Updates `docs/tools.md` to be the authoritative, comprehensive catalog of all 16 tools in `src/tools/` (including newly added `plugin_loader`). Adds accurate descriptions reflecting the real implementations delivered across prj0000013–prj0000017.

## Goal & Scope
**Goal:** Keep `docs/tools.md` in sync with actual tool implementations so contributors can discover and understand all available tools without reading source code.

**In scope:**
- `docs/tools.md` — full rewrite as structured table with accurate descriptions
- `docs/project/prj0000018/` — 9 doc artifacts

**Out of scope:**
- Changes to any `src/` code
- New tools or features

## Branch Plan
**Expected branch:** `prj0000018-documentation-assets`
**Scope boundary:** `docs/tools.md`, `docs/project/prj0000018/`
**Handoff rule:** merge after docs reviewed for accuracy
**Failure rule:** if branch mismatch, STOP
