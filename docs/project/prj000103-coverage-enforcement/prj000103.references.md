# Project prj000103: References - Coverage Minimum Enforcement

## Configuration References

### 1. pytest Configuration
**File**: `pyproject.toml`
**Section**: `[tool.pytest.ini_options]`
**Purpose**: Pytest runtime configuration

**Current State**: Has basic pytest configuration
**Integration Points**:
- `testpaths`: Directories to search for tests
- `addopts`: Additional command-line options
- `markers`: Test markers for categorization

**Coverage Integration**:
- Add `--cov=src` to measure src directory
- Add `--cov-report=html` for HTML reports
- Add `--cov-report=xml` for CI integration
- Add `--cov-fail-under=80` to enforce minimum

### 2. Coverage.py Configuration
**Package**: `pytest-cov` (already in dev dependencies)
**Purpose**: Measure test coverage

**Configuration Sections**:
- `[tool.coverage.run]`: Coverage measurement
- `[tool.coverage.report]`: Report generation
- `[tool.coverage.html]`: HTML report output
- `[tool.coverage.xml]`: XML report output

## CI/CD Integration References

### GitHub Actions Workflow
**Location**: `.github/workflows/` directory
**Pattern**: Standard Python testing workflows

**Existing Workflows**:
- `ci.yml`: Main CI pipeline
- `test.yml`: Test execution

**New Workflow**: `coverage.yml`
- Runs pytest with coverage collection
- Uploads to Codecov/Codacy (optional)
- Generates HTML and XML reports
- Comments on PRs with coverage info

### Codecov Integration (Optional)
**Purpose**: Track coverage over time
**Action**: `codecov/codecov-action`
**Files**: `coverage.xml`

## Test Infrastructure References

### Pytest Structure
**Directory**: `tests/`
**Framework**: pytest + fixtures

**Coverage Collection**:
- Automatic via `--cov` flag
- Branch coverage via `--cov-branch`
- Report generation via `--cov-report`

**New Test Module**: `tests/prj000103/test_coverage_enforcement.py`
- Validates coverage configuration
- Verifies threshold settings
- Tests report generation

## Dependencies

### Existing Dependencies
- **pytest**: Already in dev dependencies
- **pytest-cov**: Already in dev dependencies
- **coverage**: Included with pytest-cov

### Optional Dependencies for Full CI/CD
- **codecov**: For cloud coverage tracking
- **python-coverage-comment-action**: For PR comments

## No Code Duplication

### Items NOT Copied
- ❌ Application source code
- ❌ Test utilities or fixtures
- ❌ Coverage measurement logic (all from pytest-cov)

### Items Created Fresh
- ✅ Coverage configuration in pyproject.toml
- ✅ GitHub Actions workflow for coverage
- ✅ Coverage validation tests

## Integration Touch Points

| Component | File | Type | Action |
|-----------|------|------|--------|
| Coverage Config | `pyproject.toml` | CONFIG | Add coverage settings |
| Pytest Config | `pyproject.toml` | CONFIG | Update with coverage flags |
| CI/CD | `.github/workflows/coverage.yml` | WORKFLOW | Create new |
| Tests | `tests/prj000103/test_coverage_enforcement.py` | TEST | Create new |

## Coverage Measurement Scope

### Source Code to Measure
- ✅ `src/` directory (all modules)
- ⚠️ `tests/` directory (optional, usually excluded)
- ❌ Excluded: Legacy code, vendor code, codeql/

### Coverage Metrics
- **Line coverage**: % of lines executed in tests
- **Branch coverage**: % of conditional branches executed
- **Minimum threshold**: 80% (configurable)

### Report Formats
- **Terminal**: Quick summary in stdout
- **HTML**: Interactive coverage report
- **XML**: Machine-readable for CI tools
- **JSON**: Alternative machine-readable format

## Dependency Graph

```
prj000103 (Coverage Enforcement)
  ├── pyproject.toml (configuration)
  ├── pytest (test runner)
  ├── pytest-cov (coverage plugin)
  ├── coverage (measurement tool)
  ├── .github/workflows/ (CI/CD)
  └── tests/ (existing)
```

## Coverage Strategy

### Minimum Thresholds
- Overall: 80%
- Branch: 75% (if enabled)
- Module: 70% minimum per module

### Exclusion Rules
- Test code: Usually excluded
- Type checking blocks: `if TYPE_CHECKING:`
- Abstract methods: `@abstractmethod`
- Main blocks: `if __name__ == "__main__"`

### Reporting
- Generate HTML for inspection
- Upload XML to Codecov for tracking
- Comment on PRs with coverage info

---
**Configuration-Only Project**: 0% code duplication
**Coverage Target**: 80% minimum
**Tools**: pytest-cov, coverage.py
**Generated**: 2026-04-06
