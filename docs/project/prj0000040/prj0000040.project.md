# prj0000040 — Core System

**Project ID:** `prj0000040`
**Branch:** `prj0000040-core-system`
**Status:** COMPLETE

## Summary

Implements structured, validated configuration primitives for the PyAgent core runtime layer.

## Deliverables

| File | Description |
|------|-------------|
| `src/core/config.py` | `AgentConfig`, `SwarmConfig`, `load_config`, `save_config` |
| `tests/test_core_config.py` | 24 unit tests — all pass |

## Links

- Plan: `plan.md`
- Design: `brainstorm.md`

## Tasks

- [x] `AgentConfig` dataclass with validation
- [x] `SwarmConfig` with agent management
- [x] `load_config` / `save_config` I/O helpers
- [x] 24 tests pass

## Status

4 of 4 tasks completed

## Definition of Done

- [x] `AgentConfig` dataclass with validation (`name`, `llm_model`, `max_tokens`, `timeout`, `enabled`, `tags`, `extra`)
- [x] `SwarmConfig` dataclass with `add_agent`, `remove_agent`, `get_agent`, `enabled_agents`, round-trip `to_dict`/`from_dict`
- [x] `load_config(path)` — loads JSON → `SwarmConfig`; raises `FileNotFoundError` / `ValueError` on bad input
- [x] `save_config(config, path)` — serialises `SwarmConfig` to JSON; creates parent dirs automatically
- [x] 24 tests pass under `pytest tests/test_core_config.py`
