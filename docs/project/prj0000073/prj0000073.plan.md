# prj0000073 — API Documentation: Plan

_Agent: @4plan | Date: 2026-03-25 | Branch: prj0000073-api-documentation_

---

## Task Breakdown

### T1 — Write `docs/api/index.md` (replace stub)
- Intro paragraph
- Base URL section
- Versioning table
- Response headers overview
- Navigation links

### T2 — Write `docs/api/authentication.md`
- API Key method (header + query param)
- JWT HS256 method (header + query param)
- DEV_MODE explanation
- WebSocket auth
- Security recommendations

### T3 — Write `docs/api/rest-endpoints.md`
- Health domain
- Metrics domain (flm + system)
- Agent Ops domain (agent-log, agent-doc, agent-memory)
- Projects domain
- Pipeline domain
- Watchdog domain
- Plugins domain

### T4 — Write `docs/api/websocket.md`
- Connection URL
- Auth
- Mermaid sequence diagram
- Message format
- Disconnect codes

### T5 — Write `docs/api/errors.md`
- HTTP status table
- WebSocket close code table
- Rate limiting section
- Example error bodies

### T6 — Write canary existence test `tests/docs/test_api_docs_exist.py`

### T7 — Validate mkdocs.yml nav and update if required

### T8 — Commit all artifacts + API docs, push, open PR

---

## Acceptance Criteria

- [ ] `docs/api/index.md` ≥ 200 words; links to all 4 detail pages
- [ ] `docs/api/authentication.md` covers API Key, JWT, DEV_MODE, WebSocket auth
- [ ] `docs/api/rest-endpoints.md` documents ≥ 17 endpoints with request/response schemas
- [ ] `docs/api/websocket.md` contains Mermaid sequence diagram and full step-by-step protocol
- [ ] `docs/api/errors.md` covers all known HTTP error codes and WebSocket close codes
- [ ] All files lint clean (no trailing whitespace, ATX headings, fenced code blocks)
- [ ] `tests/docs/test_api_docs_exist.py` passes
- [ ] PR opened and kanban updated to Review lane

---

## Validation Commands

```powershell
# Check all docs files exist
Test-Path docs/api/index.md, docs/api/authentication.md, docs/api/rest-endpoints.md, docs/api/websocket.md, docs/api/errors.md

# Run existence test
& c:\Dev\PyAgent\.venv\Scripts\Activate.ps1; pytest tests/docs/test_api_docs_exist.py -v

# Lint check (informational)
& c:\Dev\PyAgent\.venv\Scripts\Activate.ps1; python -m flake8 tests/docs/ --max-line-length=120

# MkDocs build dry-run (optional)
& c:\Dev\PyAgent\.venv\Scripts\Activate.ps1; mkdocs build --strict 2>&1 | Select-Object -Last 20
```

---

## Kanban Update: Discovery → In Sprint

Update `docs/project/kanban.md` and `data/projects.json`: move prj0000073 from Discovery → In Sprint.
