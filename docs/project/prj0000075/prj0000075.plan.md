# prj0000075 — CI Simplification: Implementation Plan
_Agent: @4plan | Date: 2026-03-25 | Branch: prj0000075-ci-simplification_

---

## Task Breakdown

### T1 — Write failing tests (`@5test`)

| Task | File | Assertion |
|------|------|-----------|
| T1-a | `tests/ci/test_workflow_count.py` | Exactly 2 yml files: `ci.yml` + `security.yml` |
| T1-b | `tests/ci/test_workflow_count.py` | `security.yml` is valid YAML with `jobs.analyze` |
| T1-c | `tests/ci/test_workflow_count.py` | `security.yml` has `security-events: write` permission |
| T1-d | `tests/ci/test_workflow_count.py` | `security.yml` uses `github/codeql-action` (init + autobuild + analyze) |
| T1-e | `tests/structure/test_ci_yaml.py` | `docs/setup.md` contains "Local Testing" heading |

### T2 — Delete 4 redundant workflows (`@6code`)

Files to delete (via `git rm`):
- `.github/workflows/core-quality.yml`
- `.github/workflows/pm.yml`
- `.github/workflows/quality.yml`
- `.github/workflows/testing-infra.yml`

### T3 — Create `security.yml` (`@6code`)

File: `.github/workflows/security.yml`
- Trigger: push/PR to main + weekly schedule
- Permissions: contents:read, security-events:write, actions:read
- Matrix: python, javascript
- Steps: checkout, codeql-action/init (with custom queries), codeql-action/autobuild, codeql-action/analyze

### T4 — Update `docs/setup.md` (`@6code`)

Append "Local Testing" section covering:
- Pre-commit: `pre-commit run --all-files`
- Full pytest: `pytest`
- Shard equivalents (same as ci.yml shards)
- Local CodeQL (optional): `C:\Dev\CodeQL\codeql database create ...`

### T5 — Update `docs/setup.md` test (`@6code`)

Add assertion to `tests/structure/test_ci_yaml.py`:
```python
def test_setup_md_has_local_testing_section() -> None:
    content = Path("docs/setup.md").read_text(encoding="utf-8")
    assert "## Local Testing" in content
```

---

## Validation Commands

```powershell
# All tests
python -m pytest tests/ci/ tests/structure/ -q

# Broader smoke
python -m pytest tests/ci/ tests/structure/ tests/docs/ -q

# Full suite
python -m pytest -q
```

---

## Sequence

1. @5test writes `test_workflow_count.py` + setup.md assertion → RED
2. @6code deletes 4 workflows, creates `security.yml`, updates `docs/setup.md` → GREEN
3. @7exec runs the full suite, confirms GREEN
4. @8ql checks `security.yml` for workflow-injection risks
5. @9git stages, commits, pushes, opens PR
