# prj0000075 — CI Simplification: Code Artifact
_Agent: @6code | Date: 2026-03-25 | Branch: prj0000075-ci-simplification_

---

## Files Changed

### Deleted (4 files)
- `.github/workflows/core-quality.yml` — broken (validate() API doesn't exist); full overlap with ci.yml
- `.github/workflows/pm.yml` — PM tests in ci.yml shard 3; template gen is not a quality gate
- `.github/workflows/quality.yml` — ruff+mypy 100% duplicated by pre-commit; Python 3.12 version mismatch
- `.github/workflows/testing-infra.yml` — structure tests in ci.yml shard 1; full-suite run duplicates ci.yml

### Created (1 file)
- `.github/workflows/security.yml` — CodeQL for Python + JavaScript; push/PR + weekly schedule; `security-events: write` permission; references `github/codeql-action` v3

### Updated (5 files)
- `docs/setup.md` — appended `## Local Testing` section: pre-commit, pytest shard equivalents, local CodeQL CLI (`C:\Dev\CodeQL`), workflow table
- `docs/project/prj0000075/prj0000075.project.md` — migrated to modern template (Project Identity, Goal & Scope, Branch Plan)
- `tests/ci/test_workflow_count.py` — NEW: 5 tests for workflow count and security.yml structure
- `tests/structure/test_ci_yaml.py` — added `test_setup_md_has_local_testing_section`
- `docs/project/kanban.md` — prj0000075: Ideas+Discovery → In Sprint; Summary Metrics updated
- `data/projects.json` — prj0000075 lane: Discovery → In Sprint; summary updated

## Security Notes

- Removed 4 workflows with implicit elevated token permissions (no `permissions:` block = repo-default, which can be `write-all`). This is a security improvement.
- `security.yml` uses explicit minimal permissions: `contents: read`, `security-events: write`, `actions: read`.
- `ci.yml` already had `permissions: contents: read` — not changed.
- No secrets or tokens introduced.
- No user input flows through workflow steps (no `${{ github.event.* }}` in `run:` steps).
