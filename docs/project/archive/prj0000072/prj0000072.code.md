# prj0000072 — websocket-reconnect-logic | @6code artifact

## Branch Gate (Mandatory)

- **Observed branch:** `prj0000072-websocket-reconnect-logic`
- **Expected branch:** `prj0000072-websocket-reconnect-logic`
- **Gate result:** PASS ─ proceeding

---

## Code Changes

### `web/hooks/useWebSocket.ts`

**Commit:** `289221a99` — `feat(prj0000072): add exponential backoff and jitter to useWebSocket`

#### Lines added / changed

| Change | Detail |
|---|---|
| `UseWebSocketOptions` interface | Added `baseDelay`, `maxDelay`, `maxRetries`; deprecated `reconnectDelay` via JSDoc |
| `reconnectAttempts` state | `const [reconnectAttempts, setReconnectAttempts] = useState(0)` |
| Ref guards | `baseDelayRef`, `maxDelayRef`, `maxRetriesRef` — prevent stale closure captures |
| Backoff on close | `exp = min(maxDelay, baseDelay × 2^attempt)`; `jittered = exp × (0.5 + random() × 0.5)` |
| `maxRetries` guard | `if (attempt >= maxRetriesRef.current) return;` |
| `attempt` / state reset on open | `attempt = 0; setReconnectAttempts(0); setConnected(true)` |
| Return value | `{ send, connected, reconnectAttempts }` (added `reconnectAttempts`) |

#### Code health notes

- No new external dependencies.
- Stale closure pattern uses `useRef` — consistent with existing `onMessageRef` pattern already in the file.
- `clearTimeout` on cleanup — no timer leaks on component unmount.
- Type-safe: `ReturnType<typeof setTimeout>` used for the timer ref.

### `web/hooks/useWebSocket.test.ts`

- 9 new tests added in 3 `describe` blocks (see `prj0000072.test.md`).
- Uses `vi.spyOn(Math, 'random')` for deterministic jitter-bound tests.
- No DOM, no jsdom, no test renderer — runs in Node environment.

---

## Code Health Score

| Dimension | Assessment |
|---|---|
| Complexity | Low — single file, single hook, pure logic change |
| Coupling | None — hook has no new imports |
| Test coverage | Structural + formula + compat; DOM integration deferred |
| Backwards compat | Full — zero breaking changes |
| Security surface | None — no new I/O, no new network paths |
