# prj0000073 — api-documentation

## Legacy Project Overview Exception

This project overview was authored before the modern project template was established.
It pre-dates the `Project Identity`, `Project Overview`, `Branch Plan`, and
associated structured-field requirements introduced in prj0000074.

**Historical context:** prj0000073 was created and merged (PR #210) before the
workspace-meta-improvements initiative standardised the project overview format.
No corrective migration is required — the project is in the Released lane.

## Goal
Create comprehensive, hand-crafted API reference documentation in `docs/api/` covering:
- All backend FastAPI REST endpoints (routes, methods, request/response bodies, status codes)
- WebSocket protocol (connection flow, message schemas, auth handshake, reconnect)
- Authentication (API-key header, JWT bearer, DEV_MODE behaviour)
- Error codes catalogue
- A top-level `docs/api/index.md` with navigation and overview

## Scope boundary
- Read and document existing `backend/` code: `app.py`, `auth.py`, `rate_limiter.py`, `watchdog.py`, `logging_config.py`, `memory_store.py`
- Output only markdown files under `docs/api/`
- Do NOT modify any Python source code
- Do NOT add `mkdocs.yml` nav entries (that is a separate concern)

## Branch Plan
- **Expected branch:** `prj0000073-api-documentation`
- **Goal:** Produce human-readable API reference docs
- **Scope boundary:** `docs/api/` only — no source code changes
- **Handoff rule:** All docs written and reviewed via @8ql before @9git opens PR
- **Failure rule:** If any endpoint is undocumented, @6code must fill the gap before @9git proceeds

## Acceptance Criteria
1. `docs/api/index.md` exists with a table of contents
2. Each endpoint documented: route, method, auth requirement, request schema, response schema, status codes
3. WebSocket connection protocol documented with sequence diagram (mermaid)
4. Error codes table present
5. All markdown passes `markdownlint` or equivalent lint check
6. No placeholder "TODO" sections remain
