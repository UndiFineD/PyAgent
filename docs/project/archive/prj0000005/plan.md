# LLM Swarm Architecture Plan

> **2026-03-19:** Define and document the architectural foundation for a decentralized, P2P, CRDT-powered swarm of autonomous agents.

**Goal:** Create a documented, testable implementation plan for building a **swarm architecture** that supports:
- **Distributed coordination (P2P)**
- **Conflict-free replicated state (CRDT)**
- **Secure agent identity & encrypted transport**
- **Modular plugin/agent system**

This project is intentionally focused on laying a stable foundation: design documentation + incremental, tested implementation steps.

---

## 1) Scope & Acceptance Criteria

### In scope (this phase)
- Define a clear architecture and module layout for a P2P agent swarm.
- Provide a minimal prototype that demonstrates:
  -`rust_core` P2P runtime (libp2p) that can connect peers and exchange messages.
  - CRDT-backed shared state using Automerge (Rust).
  - Python bridge (`core/crdt_bridge.py`) that can merge state and return deterministic results.
  - Secure messaging with key material and authenticated encryption.
- Provide documentation and tests validating each step.

### Acceptance criteria
- A documented architecture plan lives in `docs/project/prj005-llm-swarm-architecture/`.
- A deterministic prototype exists (Rust binary + Python bridge) that merges JSON state via CRDT and is covered by tests.
- The project’s plan file includes a clear task checklist and is reflected in `prj005-llm-swarm-architecture.project.md`.

---

## 2) Task Breakdown (TDD-style)

### Phase 1 — Concept & design documentation (no code changes yet)
- [x] Create `docs/project/prj005-llm-swarm-architecture/brainstorm.md` with a clear swarm architecture overview.
- [x] Expand `docs/project/prj005-llm-swarm-architecture/plan.md` into this actionable checklist with file paths and test names.
- [x] Ensure `docs/project/prj005-llm-swarm-architecture/prj005-llm-swarm-architecture.project.md` reflects the plan checklist by regenerating the dashboard.

### Phase 2 — Rust P2P binary (proof of life)
- [x] Create `rust_core/p2p/Cargo.toml` and `rust_core/p2p/src/main.rs` (simple CLI that prints version).
- [x] Add `tests/test_rust_p2p_binary.py`:
    - Must run the built binary and assert it exits 0 and prints `p2p` or `--version`.
- [x] Add build helper `scripts/build_rust_p2p.py` (optional) to compile the crate.

### Phase 3 — Rust CRDT merge prototype
- [x] Create `rust_core/crdt/Cargo.toml` and `rust_core/crdt/src/main.rs` implementing a merge operation (Automerge or simple deterministic merge).
- [x] Add `tests/test_rust_crdt_merge.py`:
    - Calls `rust_core/crdt` binary with two JSON inputs and asserts merge output contains both keys.

### Phase 4 — Python bridge & deterministic merge
- [x] Create `core/crdt_bridge.py` with a `merge(left: dict, right: dict) -> dict` entrypoint.
- [x] Implement the bridge using a subprocess call to the `rust_core/crdt` binary.
- [x] Add `tests/test_crdt_bridge.py` verifying `merge({'a': 1}, {'b': 2})` produces a stable deterministic merge result.

### Phase 5 — Security + key management (Rust + Python)
- [x] Add `rust_core/security/Cargo.toml` and `rust_core/security/src/main.rs` implementing a CLI with `encrypt`, `decrypt`, and `rotate-keys` commands.
- [x] Add `tests/test_security_rotation.py`:
  - Validates keygen, encrypt/decrypt roundtrip, key rotation, and post-rotation decryption failure.
  - Skips automatically when binary is not built.
- [x] Add `core/security_bridge.py` with `encrypt(message: str) -> str` and `decrypt(ciphertext: str) -> str`.
- [x] Add `tests/test_security_bridge.py` verifying the Python bridge against the Rust binary.

### Phase 6 — Swarm runtime prototype (optional)
- [x] Add `agents/swarm_node.py` implementing a minimal peer that can connect to another peer and exchange a simple message.
- [x] Add `tests/test_swarm_node.py` that launches two nodes and verifies a ping/pong exchange.
- [ ] Add `scripts/run_swarm_demo.py` to launch multiple nodes locally (optional — deferred).

---

## 3) Deliverables (docs & tests)
- `docs/project/prj005-llm-swarm-architecture/plan.md` (this file)
- `docs/project/prj005-llm-swarm-architecture/brainstorm.md` (architecture sketch)
- `docs/project/prj005-llm-swarm-architecture/prj005-llm-swarm-architecture.project.md` (generated by dashboard)
- `rust_core/p2p/` + `rust_core/crdt/` + `rust_core/security/` prototypes
- `core/crdt_bridge.py` and `core/security_bridge.py`
- tests under `tests/`:
  - `test_rust_p2p_binary.py`
  - `test_rust_crdt_merge.py`
  - `test_crdt_bridge.py`
  - `test_security_rotation.py`
  - `test_security_bridge.py`
  - `test_swarm_node.py`

---

## 4) How to verify
1. Run Python tests:
   ```powershell
   pytest -q
   ```
2. Build and test Rust crates:
   ```powershell
   cd rust_core/p2p && cargo test
   cd ../crdt && cargo test
   cd ../security && cargo test
   ```
3. Confirm the project dashboard updates:
   ```powershell
   python scripts/generate_project_dashboard.py
   ```

---

## 5) Notes
- Start with the smallest functional unit (Rust binary + one bridging test) and expand.
- Keep each task small so the TDD cycle stays fast and predictable.
- The primary goal is deterministic, testable behavior (not full production performance).
