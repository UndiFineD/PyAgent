# prj0000074 — workspace-meta-improvements

## Project Identity

**Project ID:** prj0000074
**Short name:** workspace-meta-improvements
**Project folder:** `docs/project/prj0000074/`

## Project Overview

Improve overall workspace quality, agent workflow completeness, and documentation coherence
across five distinct sub-goals: agent file co-location, docstrings policy, architecture
restructure, kanban SWOT + risk register, and deeper @2think research guidelines.

## Goal & Scope

**Goal:** Meta-level workspace and agent workflow improvements — no production source code
changes, only agent definitions, memory files, architecture docs, and project tooling.

**In scope:**
- `.github/agents/*.agent.md` — update instructions (no source code)
- `docs/agents/` → `.github/agents/` — move all `*.memory.md` and `*.log.md`; update all references
- `docs/architecture/` — restructure to ≤ 8 numbered files; delete 14 old docs
- `docs/project/kanban.md` and `data/projects.json` — SWOT + Risk Register sections
- `tests/structure/test_architecture_naming.py` — new structural test
- Project planning artifacts under `docs/project/prj0000074/`

**Out of scope:**
- Python, Rust, or TypeScript source code
- Backend or frontend behaviour changes
- CI workflow files

## Branch Plan

**Expected branch:** `prj0000074-workspace-meta-improvements`
**Scope boundary:** `.github/agents/`, `docs/agents/`, `docs/architecture/`, `docs/project/kanban.md`, `data/projects.json`, `tests/structure/` — no source code
**Handoff rule:** All changes reviewed by @8ql before @9git opens PR
**Failure rule:** If any memory or log file reference is broken after the move, @6code must fix all references before @9git proceeds

## Acceptance Criteria

1. All `*.memory.md` and `*.log.md` files from `docs/agents/` exist in `.github/agents/`; no broken references remain in any other file
2. `5test.agent.md` and `6code.agent.md` contain a docstrings-required clause with ruff `D` rule guidance
3. `docs/architecture/` has ≤ 8 files, all named `N<name>.md`, no generated blobs, no `todolist.md`
4. `docs/project/kanban.md` has a risk register section and SWOT section with templates
5. `2think.agent.md` has ≥ 5 distinct research task types and a minimum-depth guideline
6. `tests/structure/` has a test asserting the architecture doc naming convention



## Milestones
Legacy milestone details are not specified in this historical document.


## Status
Legacy status details are not specified in this historical document.

