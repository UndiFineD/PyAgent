# prj0000071 — Dark Mode Accessibility: Test Plan

_Agent: @5test | Date: 2026-03-25 | Branch: prj0000071-dark-mode-accessibility_

---

## Test Strategy

Source-level static analysis: read each file and assert that the required accessibility
attributes and class tokens are present. This approach avoids requiring a browser or JSdom
and fits the existing Python test infrastructure.

---

## Test File: `tests/web/test_a11y_checklist.py`

### Suite 1 — Window.tsx assertions
- `aria-label="Minimize"` present
- `aria-label=` + `"Maximize"` or `"Restore"` present (dynamic)
- `aria-label="Close"` present
- `aria-label="Window menu"` present
- At least one `focus-visible:ring-` class present (focus indicator)

### Suite 2 — Paint.tsx assertions
- `aria-label="Pencil"` present
- `aria-label="Eraser"` present
- `aria-label="Clear canvas"` present
- `aria-label="Stroke colour"` present (color input)
- `aria-label="Stroke width"` present (range input)

### Suite 3 — Editor.tsx assertions
- `focus:ring-2` present on textarea line (or near `focus:outline-none`)

### Suite 4 — App.tsx assertions
- `focus:ring-2` present near toggle switch

### Suite 5 — ProjectManager.tsx assertions
- `role="button"` present on card
- `tabIndex={0}` present on card
- `aria-expanded` present on card
- `onKeyDown` handler present on card

### Suite 6 — themes.css assertions
- `--color-accent: #f25e77` present (not old `#e94560`)

---

## Acceptance Checklist

- [ ] All 6 suites pass with 0 failures
