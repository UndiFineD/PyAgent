# prj0000071 — Dark Mode Accessibility: Plan

_Agent: @4plan | Date: 2026-03-25 | Branch: prj0000071-dark-mode-accessibility_

---

## Task Breakdown

### T1 — Fix `web/components/Window.tsx`
- Add `aria-label` to Minimize, Maximize/Restore, Close, Menu buttons
- Add `focus-visible:ring-1 focus-visible:ring-white/60` to window control buttons

### T2 — Fix `web/apps/Paint.tsx`
- Add `aria-label` to Pencil, Eraser, Clear-canvas buttons
- Add `aria-label` to colour-picker and range inputs

### T3 — Fix `web/apps/Editor.tsx`
- Replace `focus:outline-none` with `focus:outline-none focus:ring-2 focus:ring-os-accent`

### T4 — Fix `web/App.tsx`
- Replace `focus:outline-none` on toggle switch with `focus:outline-none focus:ring-2 focus:ring-blue-400`

### T5 — Fix `web/apps/ProjectManager.tsx`
- Add `role="button"`, `tabIndex={0}`, `aria-expanded`, `onKeyDown` to project card

### T6 — Fix `web/styles/themes.css`
- Change `--color-accent` from `#e94560` to `#f25e77`

### T7 — Write `tests/web/test_a11y_checklist.py`
- Source-level checklist test: grep each file for required aria attributes
- Verify accent colour value

### T8 — Update kanban + projects.json, commit, push, PR

---

## Acceptance Criteria

- [ ] All 4 Window control buttons have `aria-label` props
- [ ] All 3 Paint tool buttons have `aria-label` props
- [ ] Paint colour and range inputs have `aria-label`
- [ ] Editor textarea has `focus:ring-2`
- [ ] App.tsx toggle has `focus:ring-2`
- [ ] ProjectManager card has `role="button"`, `tabIndex={0}`, `onKeyDown`, `aria-expanded`
- [ ] `--color-accent` is `#f25e77`
- [ ] `tests/web/test_a11y_checklist.py` passes

---

## Validation Commands

```powershell
# Run a11y checklist tests
& c:\Dev\PyAgent\.venv\Scripts\Activate.ps1; pytest tests/web/test_a11y_checklist.py -v

# Run full CI test suite
& c:\Dev\PyAgent\.venv\Scripts\Activate.ps1; pytest src/ tests/ --ignore=tests/ci -q
```
