# prj0000071 — Dark Mode Accessibility: Code

_Agent: @6code | Date: 2026-03-25 | Branch: prj0000071-dark-mode-accessibility_

---

## Files Changed

### `web/components/Window.tsx`
- Menu button: added `aria-label="Window menu"` and `aria-expanded={isMenuOpen}`; added
  `focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-white/60`
- Minimize button: added `aria-label="Minimize"`; added `focus-visible:ring-1 focus-visible:ring-white/60`
- Maximize/Restore button: added dynamic `aria-label={window.isMaximized ? 'Restore' : 'Maximize'}`; added focus ring
- Close button: added `aria-label="Close"`; added focus ring

### `web/apps/Paint.tsx`
- Pencil button: added `aria-label="Pencil"` and `aria-pressed={tool === 'pencil'}`; added focus ring
- Eraser button: added `aria-label="Eraser"` and `aria-pressed={tool === 'eraser'}`; added focus ring
- Clear canvas button: added `aria-label="Clear canvas"`; added focus ring
- Color input: added `aria-label="Stroke colour"`
- Range input: added `aria-label="Stroke width"`

### `web/apps/Editor.tsx`
- Textarea: changed `focus:outline-none` → `focus:outline-none focus:ring-2 focus:ring-os-accent`

### `web/App.tsx`
- Toggle switch: changed `focus:outline-none` → `focus:outline-none focus:ring-2 focus:ring-blue-400`

### `web/apps/ProjectManager.tsx`
- Card div: added `role="button"`, `tabIndex={0}`, `aria-expanded={expanded}`,
  `onKeyDown` handler (Enter/Space), `focus-visible:ring-1 focus-visible:ring-os-accent`

### `web/styles/themes.css`
- Changed `--color-accent: #e94560` → `--color-accent: #f25e77`
  (contrast against `--color-bg: #1a1a2e`: 5.01:1 — passes WCAG AA 4.5:1)

### `tests/web/test_a11y_checklist.py` (new)
- 17 source-level checklist tests (all pass)

---

## Test Results

```
tests/web/test_a11y_checklist.py  17 passed ✓
```

---

## WCAG Criteria Addressed

| Criterion | Level | Description | Status |
|---|---|---|---|
| 4.1.2 Name, Role, Value | A | Icon buttons now have accessible names | ✓ Fixed |
| 1.3.1 Info and Relationships | A | Card div has role/keyboard/state | ✓ Fixed |
| 2.4.7 Focus Visible | AA | Focus rings restored on textarea, toggle, card | ✓ Fixed |
| 1.3.1 Info and Relationships | A | Form inputs have accessible labels | ✓ Fixed |
| 1.4.3 Contrast (Minimum) | AA | Accent colour raised to 5.01:1 | ✓ Fixed |
