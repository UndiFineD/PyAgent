# prj0000028 — Transport T-1: Git History

## Branch
`prj0000028-transport-t1`

## What Was Done
Extended the Python transport module with high-level `NodeIdentity` and `LoopbackChannel` classes, backed by the existing Rust Ed25519 + Noise_XX implementation.

### `src/transport/__init__.py` additions
- `NodeIdentity` class — wraps `generate_node_identity()` / `get_node_id()` / `transport_sign()` / `transport_verify()`; `sign(msg)`, `verify(pub, msg, sig)` static method, `__repr__`
- `LoopbackChannel` class — creates a loopback pair, runs Noise_XX handshake automatically; exposes `send/recv` (A→B) and `send_b/recv_a` (B→A) for bidirectional testing

### Tests created
- `tests/test_transport_t1.py` — 10 Python-level tests:
  - `NodeIdentity`: public_key length, sign/verify round-trip, tampered-message rejection, repr
  - `LoopbackChannel`: send/recv, binary payload, bidirectional, empty payload, multiple messages

## Existing coverage (not modified)
- `tests/test_transport_identity.py` — 8 existing tests (Rust-level via rust_core)
- `rust_core/src/transport/identity.rs` — Ed25519 keygen, sign, verify
- `rust_core/src/transport/channel/` — Noise_XX handshake, loopback

## Commits
- `feat(prj0000028): transport T-1 - NodeIdentity + LoopbackChannel Python classes + 10 tests`

## Acceptance Criteria
- [x] `NodeIdentity` class exposes `public_key`, `sign()`, `verify()`
- [x] `LoopbackChannel` runs Noise_XX handshake automatically
- [x] All 10 new tests pass (18 total including prior tests)
